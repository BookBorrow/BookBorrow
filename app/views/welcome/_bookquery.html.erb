<ul class="list-group container-fluid col-md-offset-3">
  <% @books.each do |book|%>
  <li class="list-group-item container-fluid col-md-4 clearfix text-overflow" data-ISBN="<%=book.isbn%>">
    <%= image_tag(book.cover_url, :class => "setISBN")%>
  </li>
  <% end %>
</ul>